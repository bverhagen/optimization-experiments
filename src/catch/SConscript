Import('env')

lib_files = [
	'catch.cpp'
]

env['LIB_UNITTEST'] = [
    'catch'
]

lib = env.Library('catch', lib_files)
installed_lib = env.Install("{libs_dir}".format(libs_dir=env['LIBS_DIR']), lib)
env.Alias('unittest', installed_lib)
env.Alias('buildLibs', installed_lib)
